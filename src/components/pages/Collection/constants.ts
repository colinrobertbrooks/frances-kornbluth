type MediaConfig = {
  [key: string]: number;
};

export const listItemHeightConfig: MediaConfig = {
  xs: 200,
  sm: 150,
  md: 140,
  lg: 150,
  xl: 175,
};

export const listColConfig: MediaConfig = {
  xs: 12,
  sm: 6,
  md: 4,
  lg: 3,
  xl: 3,
};

export const LIST_ITEM_MARGIN_BOTTOM_PX = 16; // mb-3
