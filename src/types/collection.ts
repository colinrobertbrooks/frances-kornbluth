export interface ICollectionGoogleSheetRow {
  gsx$id: {
    $t: string;
  };
  gsx$name: {
    $t: string;
  };
  gsx$minimgurl: {
    $t: string;
  };
}

export interface ICollectionRecord {
  id: number;
  name: string;
  img: string;
}
